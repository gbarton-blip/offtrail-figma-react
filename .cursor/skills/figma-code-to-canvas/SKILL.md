# figma-code-to-canvas (project pointer)

This project uses the **global** skill:

`~/.cursor/skills/figma-code-to-canvas/SKILL.md`

Load that skill for all code-to-canvas work in this repo. Do not duplicate workflow rules here.

The global skill includes **Gate 1d (visual contrast)**, **Gate 1e (image fidelity)**, and **Gate 1f (fresh-run canvas attestation)**. See the global skill's `references/` folder.
